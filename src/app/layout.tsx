import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import './layout.scss'
import { LeftPanel } from "@/components/widgets/left-panel";
import { IMenuProps } from "@/components/features/menu/menu";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export interface IRootLayoutProps {
  children: Readonly<React.ReactNode>;
  params: IMenuProps

}

export default function RootLayout({ children, params: { page } }: IRootLayoutProps) {

  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="container">
          <LeftPanel page={page} /> {/* TODO Слишком длинный спуск? */}
          <div className="container__content">
          {children}
          </div>
        </div>
      </body>
    </html>
  );
}
